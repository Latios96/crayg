#include "LombokBaryCentricLombokTest.h"
LombokBaryCentricLombokTest::LombokBaryCentricLombokTest(
    float alpha,
    float beta,
    float gamma,
    std::string name
)
    :
    alpha(alpha),
    beta(beta),
    gamma(gamma),
    name(name) {}